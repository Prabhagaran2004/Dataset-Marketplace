// Network Configuration
export const NETWORKS = {
  ZG_TESTNET: {
    chainId: 16602,
    name: "0G Testnet (Galileo)",
    rpc: "https://evmrpc-testnet.0g.ai",
    indexer: "https://indexer-storage-testnet-turbo.0g.ai",
    blockExplorer: "https://chainscan-galileo.0g.ai",
    currency: "0G",
  },
  ZG_MAINNET: {
    chainId: 16661,
    name: "0G Mainnet",
    rpc: "https://evmrpc.0g.ai",
    indexer: "https://indexer-storage-turbo.0g.ai",
    blockExplorer: "https://chainscan.0g.ai",
    currency: "0G",
  },
};

export const CURRENT_NETWORK = NETWORKS["ZG_TESTNET"];

// Contract Configuration
export const MARKETPLACE_CONTRACT_ADDRESS =
  process.env.NEXT_PUBLIC_MARKETPLACE_CONTRACT || "0x0";

// Platform Configuration
export const PLATFORM_FEE_PERCENT = 2.5; // 2.5%
export const MIN_FILE_SIZE = 1024; // 1 KB
export const MAX_FILE_SIZE = 10 * 1024 * 1024 * 1024; // 10 GB

// UI Constants
export const ITEMS_PER_PAGE = 12;
export const CATEGORIES = [
  "Healthcare",
  "Finance",
  "AI/ML",
  "Real Estate",
  "E-commerce",
  "Social",
  "Climate",
  "Other",
];

// Contract ABI (simplified - full ABI would be generated by ethers)
export const MARKETPLACE_ABI = [
  "function listDataset(bytes32 rootHash, string metadataURI, uint256 price) external returns (uint256)",
  "function purchaseDataset(uint256 id) external payable",
  "function hasAccess(uint256 id, address user) external view returns (bool)",
  "function toggleDatasetStatus(uint256 id) external",
  "function updatePrice(uint256 id, uint256 newPrice) external",
  "function withdrawFunds() external",
  "function withdrawPlatformFees() external",
  "function getDataset(uint256 id) external view returns (address, bytes32, string, uint256, uint256, bool)",
  "function getSellerBalance(address seller) external view returns (uint256)",
  "function datasetCount() external view returns (uint256)",
  "event DatasetListed(uint256 indexed id, address indexed seller, bytes32 rootHash, uint256 price, string metadataURI)",
  "event DatasetPurchased(uint256 indexed id, address indexed buyer, address indexed seller, uint256 price, uint256 platformFee)",
];
